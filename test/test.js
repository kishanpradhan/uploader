var expect = require('chai').expect;

describe("Upload Stages", () => {

	it("Start: Should return cursor, progress and chunk_size", async () => {
		expect(typeof true).to.equal(typeof true);
	});

	it("Upload: Should return cursor, progress and next chunk_size", async () => {
		expect(typeof true).to.equal(typeof true);
	});

	it("Done: Should return cursor, progress and url", async () => {
		expect(typeof true).to.equal(typeof true);
	});

});
